# 答え合わせ

> 注意: このファイルには最初から答えと解法が書かれています。プレイ前に読むと謎解きの内容が分かります。

## 正解コード

```text
MI4040-21704-ARCHIVE
```

## 導き方

1. 機種ID

   企業HPでは省電力設計と `MI Green Profile` が強調されています。製品一覧を見ると、他製品は `適合` なのに、販売終了済みの `MI-4040 Legacy` だけが `確認中` になっています。

   MI-4040詳細では、搬送部制御、待機時消費電力、対象ファームウェア `Ver.2.17.04` が関連していることが分かります。機種IDはハイフンなしで `MI4040` です。

2. 停止配布番号

   サポート資料ページ下部の `旧公開物索引` は、移行前システムの自動生成項目として残っています。

   2021/04/17 の行に `旧型A3機 搬送制御最適化 Ver.2.17.04` があり、2021/04/19 に `Ver.2.17.04` の配布停止が記録されています。

   停止配布番号は `Ver.2.17.04` の数字部分をつなげた `21704` です。

3. 偽装文書

   旧公開物索引の `関連資料: 回収済み / 管理番号 MI4040-PWR-21704` は、回収済みに見えますが管理番号部分だけリンクとして残っています。

   偽装文書には「測定条件に起因する表示差異であり、製品仕様上の問題ではない」とありますが、以下の矛盾があります。

   - 測定日が 2021/04/20 なのに、作成区分が公開前測定資料になっている。
   - `MI Green Profile` 判定欄が空欄になっている。
   - 仕様値 0.9 kWh / 8h に対して確認値が 2.7 kWh / 8h で、約3倍になっている。
   - 末尾に記録保全室への移管が書かれている。

4. 保管索引

   導入事例ページには、掲載順が旧記録保全室から移管された順序に基づくと書かれています。

   導入事例の企業名を上から読むと、以下の並びになります。

   ```text
   Aoba Office
   Rindo Accounting
   Chiyoda Design
   Hayate Clinic
   Iwakiri Legal Office
   Vesta Logistics
   Enoki Systems
   ```

   頭文字をつなげると `ARCHIVE` になります。

## 最終入力

照合端末で以下を入力すると、復元ログページへ遷移します。

```text
MI4040-21704-ARCHIVE
```

大文字小文字は区別されず、前後の空白も無視されます。

## 真相の要約

MI-4040 は故障していたのではなく、搬送部制御の安全側判定が広すぎたため、紙を送っていない時間にも補正動作を続けていました。実測値は仕様書の約3倍で、環境表示が消えたのは誤記ではなく、表示できなかったからです。
